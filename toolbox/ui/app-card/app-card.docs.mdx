---
description: "App card: icon, name, subtitle, star rating, domain badges and a featured ribbon, linking to the app's detail page."
labels: ["app-card", "toolbox", "card"]
---

The `AppCard` component renders a single coping-app catalog entry in the toolbox marketplace grid: an icon, name, subtitle, star rating, coping-domain badges and an optional "community featured" ribbon. The whole card links to the app's detail page.

## Basic usage

```tsx
<AppCard
  icon="🫧"
  name="נשימה רגועה"
  subtitle="תרגילי נשימה מודרכים להרגעה מיידית"
  avgRating={4.7}
  ratingCount={96}
  clickCount={1240}
  isFeatured
  href="/toolbox/breathe-calm"
  domains={[
    { id: 'mindfulness-breathing', slug: 'mindfulness-breathing', name: 'מיינדפולנס ונשימות' },
    { id: 'anxiety', slug: 'anxiety', name: 'חרדה' },
  ]}
/>
```

## App without reviews yet

When an app has no ratings yet, the card shows an encouraging message instead of a star rating.

```tsx
<AppCard
  icon="📓"
  name="מצב רוח"
  subtitle="יומן רגשי פשוט למעקב וויסות"
  avgRating={0}
  ratingCount={0}
  clickCount={640}
  isFeatured={false}
  href="/toolbox/mood-map"
  domains={[{ id: 'emotional-regulation', slug: 'emotional-regulation', name: 'ויסות רגשי' }]}
/>
```

## Rendering a grid of apps

`AppCard` is designed to be rendered in a responsive grid, one card per app in the catalog.

```tsx
<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))', gap: 20 }}>
  <AppCard
    icon="🪨"
    name="קרקוע"
    subtitle="כלים להתמודדות עם טריגרים ופלאשבקים"
    avgRating={4.9}
    ratingCount={143}
    clickCount={1520}
    isFeatured
    href="/toolbox/ground-me"
    domains={[{ id: 'triggers', slug: 'triggers', name: 'טריגרים' }]}
  />
  <AppCard
    icon="🌙"
    name="עוגן לילה"
    subtitle="סיפורי הרדמה ונופי סאונד לשינה עמוקה"
    avgRating={4.3}
    ratingCount={58}
    clickCount={870}
    isFeatured={false}
    href="/toolbox/sleep-anchor"
    domains={[{ id: 'sleep', slug: 'sleep', name: 'שינה' }]}
  />
</div>
```
