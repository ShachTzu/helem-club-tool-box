---
description: 'A helam-prototype app.'
labels: ['app', 'react']
---

A React application for helam-prototype. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use helam-prototype 
```

To run the app locally, run the following:

```
bit run helam-prototype 
```
