---
description: "RTL select / multi-select list with options {value,label}, search, and chip display for multi."
labels: ["select", "dropdown", "multi-select", "filter", "rtl"]
---

`SelectList` is an RTL-first select / multi-select control used for domain selection and filters across Helam Club. It supports single or multiple selection, an optional search input, and chip display for selected values in multi mode.

## Basic single select

Use `value` and `onChange` with a plain string for single-selection mode.

```tsx
function BasicExample() {
  const [value, setValue] = React.useState('anxiety');

  const options = [
    { value: 'anxiety', label: 'חרדה' },
    { value: 'sleep', label: 'שינה' },
    { value: 'work-career', label: 'עבודה וקריירה' },
  ];

  return (
    <SelectList
      label="בחירת תחום"
      options={options}
      value={value}
      onChange={(next) => setValue(next)}
    />
  );
}
```

## Multi-select with chips

Set `multiple` to allow selecting several options. Selected options are displayed as chips inside the trigger, each removable individually.

```tsx
function MultiExample() {
  const [values, setValues] = React.useState(['anxiety', 'sleep']);

  const options = [
    { value: 'anxiety', label: 'חרדה' },
    { value: 'sleep', label: 'שינה' },
    { value: 'mindfulness-breathing', label: 'מיינדפולנס ונשימות' },
  ];

  return (
    <SelectList
      label="סינון לפי תחומי התמודדות"
      options={options}
      value={values}
      onChange={(next) => setValues(next)}
      multiple
    />
  );
}
```

## Search and custom placeholders

Search is enabled by default. You can customize the placeholder texts to fit your context, or disable search entirely with `searchable={false}`.

```tsx
function SearchExample() {
  const [values, setValues] = React.useState([]);

  const options = [
    { value: 'anxiety', label: 'חרדה' },
    { value: 'sleep', label: 'שינה' },
    { value: 'guilt-shame', label: 'אשמה, בושה וביקורת עצמית' },
  ];

  return (
    <SelectList
      label="דומיינים רלוונטיים"
      placeholder="בחרו תחומים..."
      searchPlaceholder="חיפוש תחום..."
      options={options}
      value={values}
      onChange={(next) => setValues(next)}
      multiple
    />
  );
}
```

## Keyboard and screen readers

The control follows the ARIA select-only combobox pattern, as required by IS 5568 (WCAG 2.0 AA, 2.1.1 Keyboard and 4.1.2 Name, Role, Value).

| Key | Action |
| --- | --- |
| `Tab` | moves focus to the control |
| `Enter` / `Space` / `↓` / `↑` | opens the list, or selects the active option when already open |
| `↓` / `↑` | moves the active option, wrapping at the ends |
| `Home` / `End` | jumps to the first / last option |
| `Esc` | closes the list and returns focus to the control |

The trigger is exposed as `role="combobox"` with `aria-expanded`, `aria-haspopup="listbox"` and `aria-activedescendant`; the popup is a `role="listbox"` of `role="option"` items carrying `aria-selected`. Pass a `label` so the control has an accessible name — it is wired to the combobox via `aria-labelledby`.
