RELEASE CANDIDATE — אונבורדינג חברי קהילה + פלטפורמת אישור

ענף: `claude/form-member-onboarding-451813`, מבוסס על Step 5 (`3fe37b5`).
מבוסס על שאלות 1‑9 ו‑12 בטופס המונדיי "בניית ארגון המתנדבים של הלם קלאב".

Ponytail mode used: full
Experts run: data-migration (מול הדאטה האמיתי), red-team-engineering (ידני, על הקוד שרץ),
  self-review על הדיפ, בדיקת קצה‑לקצה חיה
Skipped on purpose: housekeep-max ו‑a11y-review כמטאטא מלא — הרצתי במקומם בדיקת הרשאות
  ידנית מול השרת ובדיקת נגישות ממוקדת על מה שהוספתי. הממצא שיצא מזה רשום למטה.
Blocking: 0   Non-blocking: 2 (נגישות SelectList, מחיקת פרופיל)
**מוכן למיזוג לענף** — הזרימה נבדקה מקצה לקצה על אפליקציה שרצה, 117/117 בדיקות עוברות
ו‑check-types נקי. **לא מוכן לפרודקשן** עד שיסגרו שני ה‑CONFIG למטה.

---

## WHAT SHIPPED

מי שנרשם לאתר — במייל או בגוגל — ממלא פעם אחת שאלון בן 4 מסכים, ועובר ל"ממתין לאישור".
אדמין רואה אותו ב‑`/admin` → "אישור חברי קהילה", פותח את הפרטים, מאשר או דוחה.
באישור נשלח מייל וארגז הכלים נפתח.

מייל וגוגל הם אותו מסלול. מה שהחשבון כבר יודע ממולא מראש.

## PLATFORMS
touched: web (RTL) · UAT already run on: דפדפן דסקטופ, מקומי. **מובייל לא נבדק.**

## DATA
אוסף חדש `memberprofilemodels`. שדה לכל שאלה מ‑1‑9 ו‑12.
**שאלות 8 ו‑9 (פציעה, הכרה) הן מידע רפואי** — מידע רגיש לפי חוק הגנת הפרטיות.

- אוסף נפרד, לא שדות על רשומת המשתמש.
- שלוש צורות GraphQL: `MyMembership` (הסטטוס שלך), `MemberProfileSummary` (שורת תור,
  **בלי** שדות רפואיים), `AdminMemberProfile` (הכול — חבר אחד בכל פעם).
- **נבדק חי:** תיאור הפציעה לא נמצא ב‑DOM כשהתור נטען, ומופיע רק אחרי לחיצה על
  "הצג פרטים". כלומר רשימת התור באמת לא מביאה מידע רפואי.
- הבעלים של פרופיל תמיד מה‑session, אף פעם לא מהקלט.
- retention: **אין מדיניות מחיקה, ואין מסלול "מחקו אותי"** — ראו KNOWN OPEN.

## MIGRATION
אוסף חדש, בלי שינוי סכימה קיימת. הפיך: מוחקים את האוסף.
לכל חשבון קיים נוצרת שורה `none` בטעינת עמוד ראשונה, ואז הוא מופנה לאונבורדינג —
**משתמשים קיימים יעברו אונבורדינג בכניסה הבאה.**

`helam_dev` נבדק: 5 משתמשים, 11 כלים, אבל ארבעה מהמשתמשים הם `@example.com` ושלושה
מארבעת מגישי הכלים אינם משתמשים כלל — זו סביבת פיתוח עם דאטה מומצא.
`onboardingCompleted` הקיים על UserModel לא מיובא בכוונה: הוא נכתב על ידי זרעים, וייבוא
ממנו היה נותן "ממתין לאישור" למי שלא מילא כלום.

## BREAKING
`ToolboxNode` קיבל ארגומנט קונסטרקטור רביעי (`membership`) — ה‑spec עודכן.
`useOnboarding` שינה חוזה: `completeOnboarding` מחזיר Promise, `resetOnboarding` נמחק,
`mockCompleted` → `mockStatus`. נצרך רק ב‑`onboarding-gate.tsx` וב‑`onboarding-page`.

## CONFIG
אין משתני סביבה חדשים. מייל האישור רוכב על `RESEND_API_KEY` הקיים.

**שני חסמי פרודקשן שאינם בקוד הזה, אבל בלעדיהם הפיצ'ר חסר משמעות:**
- `GOOGLE_CLIENT_ID` **חסר באתר החי.** הוכחה: `authConfig` מחזיר `googleClientId: null`,
  ולכן אין כפתור גוגל. מקומית, עם המשתנה, הכפתור מופיע.
- `RESEND_API_KEY` **לא תקין באתר החי** — שגיאת "לא הצלחנו לשלוח את הקוד" היא בדיוק ענף
  כשל השליחה. חשד: המפתח הישן נמחק ולא הוחלף.

בלי אלה אי אפשר בכלל להירשם, ולכן אי אפשר להגיע לאונבורדינג.

## ROLLBACK
להסיר `MembershipAspect` מ‑`helam.bit-app.ts` ולהחזיר `requireMember` ל‑`requireUser`
בשני מקומות ב‑toolbox. האוסף יכול להישאר.

## WATCH
כמה תקועים ב‑`none` — אם רבים נרשמים ולא מסיימים, האונבורדינג ארוך מדי.
כמה תקועים ב‑`pending` יותר מכמה ימים — אין מי שמאשר.
מספר שאומר לבטל: אחוז מסיימים מתוך נרשמים מתחת ל‑50%.

## KNOWN OPEN
- **אין מחיקת פרופיל / "מחקו אותי".** שומרים מידע רפואי בלי מסלול מחיקה. צריך להיסגר
  לפני שיש חברים אמיתיים. (Claude)
- **`SelectList` אינו נגיש במקלדת** — ה‑trigger הוא `div` בלי `role` ובלי `tabindex`,
  אז הוא לא ממוקד ולא מוכרז בקורא מסך. **תקלה קיימת בקומפוננטת העיצוב, לא שלי** — אבל
  הוספתי שתי הופעות שלה (מגדר, הכרה) על מסלול חובה. תחת תקן ישראלי 5568 זו אי‑עמידה.
  התיקון שייך ל‑`design/inputs/select-list` ומשפיע על כל השימושים. (Claude)
- דירוג ותגובות עדיין לא דורשים חברות מאושרת — רק הגשה. (Claude)
- שאלות 10‑11 לא נכללו, לפי בקשה. (Shachar)
- Step 5 שמתחתיי מעולם לא נסקר על ידי אף אחד. הבדיקות שלו עוברות. (Step 5 session)
- לוח הבקרה מציג מדדים קבועים ומומצאים (47 חברים, 312 תגובות...). לא שלי, אבל זה
  מוצג לאדמינים כאילו הוא אמיתי. (לא מוקצה)

## NOT TESTED
- **מובייל** — שום מסך לא נבדק ברוחב טלפון.
- **הרשמה דרך גוגל** — נבדקה רק במייל OTP. המסלול המשותף אותו קוד, אבל לא הורץ.
- **קורא מסך** — לא הורץ. ראו ממצא ה‑SelectList למעלה.
- **מייל אמיתי** — `RESEND_API_KEY` לא היה מוגדר מקומית, אז המיילים נכתבו לקונסולה
  בלבד. התוכן והנמען אומתו; המסירה בפועל לא.
- **דחייה** — אישור נבדק מקצה לקצה, דחייה לא (אותה מוטציה, ענף אחר).
- **ביטול חברות למי שכבר אושר** — לא נבדק.
- **ריבוי אדמינים / החלטות במקביל** — לא נבדק.
- **לאיזה מסד נתונים האתר החי מחובר — לא ידוע.** נבדק מול `helam_dev`.
- `bit snap` / `bit export` **לא בוצעו** — הענף לא על הלֵיין.

## מה כן נבדק חי, מקצה לקצה
הרשמה במייל → הפניה אוטומטית לאונבורדינג → מילוי 4 מסכים (שם ומייל ממולאים מראש
מהחשבון) → שליחה → `pending` במסד עם כל השדות → **חסימה מהגשת כלי, גם ב‑UI וגם בשרת**
(קריאה ישירה ל‑`submitToolboxApp` עם session תקין החזירה `AccessDenied`) → אדמין רואה
בתור, המידע הרפואי נמשך רק בלחיצה → אישור → `approved` במסד + מייל "ברוכים הבאים" →
אותה מוטציה בדיוק עוברת בהצלחה.

טלפון בפורמט בינלאומי `+972-54-1234567` נשמר כ‑`0541234567`.
נתוני הבדיקה שיצרתי נמחקו; המסד חזר ל‑5 משתמשים ו‑11 כלים.
